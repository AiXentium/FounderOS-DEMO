node_modules/
.next/
.next-*/
data/*.db
data/*.db-*
# External template downloads are local cache material; the catalog stores audited source links.
data/template-vault/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.worktrees/
.playwright-mcp/
claude code images/
dir-sidebar.jpeg

# Firecrawl scratch (scraped API docs, tokens) — never commit.
.firecrawl/

# Secrets — never commit real credentials. Only .env.example is tracked.
.env
.env.local
.env.*.local
